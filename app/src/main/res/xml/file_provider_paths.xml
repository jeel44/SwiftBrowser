<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External files directory for user-visible downloads -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- External storage downloads folder -->
    <external-path
        name="external_storage"
        path="Download/" />

    <!-- App-specific cache for temporary share files -->
    <cache-path
        name="cache"
        path="." />
</paths>
