<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for web browsing and locker biometrics -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WebRTC Permissions for in-browser video/audio conferencing -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Geolocation Permissions for maps and delivery websites -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Storage and Media Permissions for Locker & Downloader -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- Notifications for download progress on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Allow handing off downloaded APKs to the package installer (Android 8+) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Haptic feedback vibration permission -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Read Google accounts from AccountManager for the native OAuth account picker.
         This is a NORMAL permission (not dangerous) — no runtime dialog needed. -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- Declare package visibility queries for resolving custom scheme intents (Android 11+) -->
    <queries>
        <!-- Orbot (Tor proxy) — required for package visibility on Android 11+ -->
        <package android:name="org.torproject.android" />

        <!-- Generic ACTION_VIEW catch-all — ensures chooser can discover apps for any scheme -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>

        <!-- For querying UPI payment apps -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>

        <!-- Specific UPI app packages (required for chooser visibility on Android 11+) -->
        <package android:name="com.google.android.apps.nbu.paisa.user" /> <!-- Google Pay -->
        <package android:name="com.phonepe.app" />                       <!-- PhonePe -->
        <package android:name="net.one97.paytm" />                       <!-- Paytm -->
        <package android:name="in.org.npci.upiapp" />                    <!-- BHIM -->
        <package android:name="com.dreamplug.androidapp" />              <!-- CRED -->
        <package android:name="in.amazon.mShop.android.shopping" />      <!-- Amazon Pay -->
        <package android:name="com.whatsapp" />                          <!-- WhatsApp -->

        <!-- External download managers (ADM / 1DM / FDM / NDM / Navi / Flud / Aria2, etc.) — for chooser visibility on Android 11+ -->
        <package android:name="com.dv.adm" />                              <!-- ADM (legacy) -->
        <package android:name="com.dv.adm.pay" />                          <!-- ADM Pro (legacy) -->
        <package android:name="com.dv.get" />                              <!-- ADM (current) -->
        <package android:name="com.dv.get.pro" />                          <!-- ADM Pro (current) -->
        <package android:name="idm.internet.download.manager" />           <!-- 1DM -->
        <package android:name="idm.internet.download.manager.plus" />      <!-- 1DM+ -->
        <package android:name="idm.internet.download.manager.lite" />      <!-- 1DM Lite -->
        <package android:name="idm.internet.download.manager.adm" />       <!-- 1DM+ (alt) -->
        <package android:name="idm.internet.download.manager.adm.thirdparty" /> <!-- 1DM third-party -->
        <package android:name="com.idm.internet.download.manager" />       <!-- 1DM (alt) -->
        <package android:name="org.freedownloadmanager.fdm" />              <!-- Free Download Manager -->
        <package android:name="com.al.ndm" />                              <!-- NDM -->
        <package android:name="com.tt.android.dm" />                       <!-- Download Navigator -->
        <package android:name="com.pavelrekun.yak2" />                     <!-- Download Navi -->
        <package android:name="org.mrb.downloadmanager" />                 <!-- Download Manager -->
        <package android:name="com.gianlu.aria2android" />                 <!-- Aria2Android -->
        <package android:name="com.gigatools.downloader" />                <!-- Nova Download Manager -->
        <package android:name="com.ponydroid.ponydroid" />                 <!-- Ponydroid -->
        <package android:name="com.mixplorer" />                           <!-- MiXplorer -->
        <package android:name="com.mixplorer.silver" />                    <!-- MiXplorer Silver -->

        <!-- For querying torrent and magnet links (Flud, BitTorrent, uTorrent, LibreTorrent, etc.) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="magnet" />
        </intent>
        <package android:name="com.delphicoder.flud" />
        <package android:name="com.delphicoder.flud.paid" />
        <package android:name="com.bittorrent.client" />
        <package android:name="com.bittorrent.client.pro" />
        <package android:name="com.utorrent.client" />
        <package android:name="com.utorrent.client.pro" />
        <package android:name="org.proninyaroslav.libretorrent" />
        <package android:name="hu.tagsoft.ttorrent.lite" />
        <package android:name="hu.tagsoft.ttorrent.pro" />
        <package android:name="com.torrent.torrdroid" />

        <!-- For intent:// scheme links (payment gateways like Razorpay, PayU) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="intent" />
        </intent>

        <!-- For opening any downloaded file (PDF, APK, Video, Audio, Image, Zip, Text) on Android 11+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSTALL_PACKAGE" />
        </intent>

        <!-- For market:// links (Play Store) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>

        <!-- For querying email apps -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- Also handle mailto: via ACTION_VIEW -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>

        <!-- For dialer -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>

        <!-- For SMS -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>

        <!-- WhatsApp / Telegram deep links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>

        <!-- Geo/maps links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:name=".OmniApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:resizeableActivity="true"
        android:largeHeap="true"
        android:theme="@style/Theme.OmniBrowser">


        <!-- Opt out of ML Kit telemetry for Google Play Data Safety compliance -->
        <meta-data
            android:name="com.google.mlkit.telemetry.opt_out"
            android:value="true" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:theme="@style/Theme.OmniBrowser"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout">
            
            <!-- Standard intent filters to handle web pages and HTTP/HTTPS links -->
            <intent-filter android:priority="1">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:scheme="about" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="android.intent.action.WEB_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".MainActivityDefault"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityDark"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/ic_launcher_dark"
            android:roundIcon="@mipmap/ic_launcher_dark_round"
            android:label="@string/app_name"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityLight"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/ic_launcher_light"
            android:roundIcon="@mipmap/ic_launcher_light_round"
            android:label="@string/app_name"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityAuraDark"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/ic_launcher_ring_dark"
            android:roundIcon="@mipmap/ic_launcher_ring_dark_round"
            android:label="@string/app_name"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityAuraLight"
            android:targetActivity=".MainActivity"
            android:icon="@mipmap/ic_launcher_ring_light"
            android:roundIcon="@mipmap/ic_launcher_ring_light_round"
            android:label="@string/app_name"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>



        <!-- Secure file provider to allow temporary viewing of decrypted PDFs or downloads -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.rebelroot.omni.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- WireGuard VPN backend service -->
        <service
            android:name="com.wireguard.android.backend.GoBackend$VpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- Disable and remove GeckoView's CrashHelper service to prevent NullPointerException
             on startup (Mozilla Bug 1963220) which triggers the "Omni Browser keeps stopping" dialog.
             Crash handling is managed in-process via Thread.setDefaultUncaughtExceptionHandler. -->
        <service
            android:name="org.mozilla.gecko.crashhelper.CrashHelper"
            tools:node="remove" />
    </application>

</manifest>
